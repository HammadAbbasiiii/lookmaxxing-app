# LookMaxx frontend environment variables (placeholders only — no secrets here).

# Base URL of the FastAPI backend. Falls back to the production Render origin.
NEXT_PUBLIC_API_URL=https://lookmaxx-api.onrender.com/api/v1

# No backend secrets (SECRET_KEY, STRIPE_SECRET_KEY, DEEPSEEK_API_KEY,
# CLOUDINARY_API_SECRET) may ever appear here or anywhere in the client bundle.
