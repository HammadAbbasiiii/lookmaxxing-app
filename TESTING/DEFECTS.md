# Defects & Findings — LookMaxx

> Running log of every defect/finding discovered, triaged, and (where fixed)
> regression-covered during this pass. Severity model: P0 critical / P1 high /
> P2 medium / P3 low.

## FIXED this pass

| ID | Severity | Description | Fix | Regression test |
|---|---|---|---|---|
| DEF-001 | P2 (a11y) | Signup consent "Terms"/"Privacy Policy" inline links had 1.2:1 contrast vs surrounding muted text (axe `link-in-text-block`, needs 3:1 or a non-color indicator). | Added `underline underline-offset-2` to both links in `SignupForm.tsx`. | e2e/accessibility.spec.ts (signup) |
| DEF-002 | P3 | `GET /health` reported a nonsensical `max_rss_mb` (~140880) on macOS — `_get_memory_usage` treated all POSIX as Linux (KB) but macOS reports `ru_maxrss` in bytes. | Platform-aware divisor (`darwin` → bytes/1024², else KB/1024). | manual curl + no regression in test_health |
| DEF-003 | P3 | E2E suite's cumulative anonymous traffic tripped the (correct) 60/min anonymous rate limit, causing spurious 429s that masked 401/200 in unrelated tests. | Made `ANONYMOUS_LIMIT`/`AUTHENTICATED_LIMIT` env-overridable (`RATE_LIMIT_*`); E2E runs with `RATE_LIMIT_ANONYMOUS=1000`. Production defaults unchanged. | e2e/security-headers.spec.ts, auth.spec.ts |
| DEF-004 | P2 (security) | `npm audit` flagged 2 vulns (1 high, 1 moderate) — `postcss@8.4.31` bundled inside `next` (XSS via unescaped `</style>`; arbitrary file read via `sourceMappingURL`). Build-time only; no untrusted CSS input. | Non-breaking fix: npm `overrides` pinning `next → postcss` to patched `8.5.27` (root devDep resolves to `8.5.28`). Avoids the breaking `next@16` upgrade. `npm audit` → 0 vulns. | `npm audit` + production build + full Chromium E2E (43 passed) |
| DEF-007 | P1 (billing) | `POST /payments/change-plan` granted the new plan using the **pre-switch** `current_period_end`. A monthly→annual switch (e.g. Pro→Elite annual) therefore under-granted access by ~11 months — a paying user could be treated as expired on the old monthly anchor until the next `customer.subscription.updated` webhook arrived. | Read the authoritative expiry from the `Subscription.modify` response — `new_period_end = _obj_period_end(updated) or period_end` — instead of the stale pre-switch value. | `scripts/payments_e2e.py` scenario 2 asserts the annual upgrade expiry extends ~365d; backend pytest 295 passed |
| DEF-008 | P1 (billing) | Stripe API `2026-08-26.dahlia` (lib `stripe` 15.6.1) moved `current_period_end` **off** the subscription top level onto `items.data[].current_period_end` (empirically verified: top-level is `None`), and invoice lines carry **no** `price` key — the price id lives at `pricing.price_details.price`. Webhook grant paths therefore read a `None` expiry, and invoice events fell back to the default tier. | `_obj_period_end()` / `_sub_period_end()` fall back to `items.data[0].current_period_end`; `_tier_from_obj()` falls back to `pricing.price_details.price` for invoice lines. | `scripts/payments_e2e.py` (8 scenarios / 52 checks, incl. the optional-property assertions); backend pytest 295 passed |
| DEF-010 | P1 (mobile) | The mobile account drawer was rendered *inside* the sticky `<header>`. `backdrop-blur` on the header is a `backdrop-filter`, which makes that element the containing block for `position: fixed` descendants — so the drawer's `fixed inset-0` overlay resolved against the 64px header box, its `overflow-hidden` clipped every menu row, and the header's `z-40` stacking context trapped the overlay *beneath* the bottom tab bar. On every phone viewport the menu opened as a 64px sliver (name + email + ✕) with Profile / Settings / Upgrade / Log out unreachable. Measured on the pre-fix build at 390×844: overlay height **64px** instead of 844px, rows laid out at y≈72–200, and `elementFromPoint` at each row's centre returning page content. | Moved `<AvatarDrawer>` out of `<header>` — `TopNav` now returns a fragment whose children are the header and the drawer (siblings), with a comment stating why it must never go back inside. Also gave the drawer safe-area bottom padding (it now covers the tab bar that used to absorb it) and a scrollable menu. No change to the desktop dropdown. | `e2e/account-drawer.spec.ts` (5 tests: unclipped geometry + tap reachability, Profile/Upgrade routing, Escape + scroll-lock release, drawer above the bottom tabs). Validated **red on the pre-fix build (4/5 fail)** and **green on the fixed build (5/5)**. |
| DEF-011 | P2 (copy) | The top-line monetization copy sold an Elite perk — "1:1 coach Q&A + priority" — that has no endpoint, no UI and no entry in HEAD's `FEATURES` list (it survives only in the pre-2026-09-16 docs and a stale local build), while the *shipped* Elite value prop (harmony map, weekly blueprint, share card, Day-90 reveal, movie) appeared nowhere in the monetization summary. Tier docs and code disagreed about what Elite even is. | Rewrote `PRODUCT_SPEC.md` §12.1 + §12.2 and `docs/PREMIUM_ENTITLEMENTS.md` §2 to the enforced matrix (feature → min tier → enforcing dependency → surface), and added the rule "FEATURES describes shipped behaviour only". | Endpoint probe with free/Pro/Elite tokens (`TESTING/TEST-RESULTS.md` → "Pro vs Elite matrix audit") |
| DEF-012 | P2 (monetization UX) | Four things the UI sold as Pro are served to free members (`GET /plan`, `POST /plan/checkin`, `GET /progress/*`, `GET /products/*`, `GET /arc/state`) — so "Upgrade to Pro" promised features a free user already had — while genuinely locked things were dead ends: /arc showed a "Pro" badge with no CTA and /glowups an "Elite" badge with no CTA. Menus gave no advance warning either: Coach (a Pro-only page), Insights and Simulator (Pro) looked identical to free destinations, and the dashboard's locked-perk card listed Pro and Elite perks in one unlabelled list under a bare "Upgrade" badge. | Nav hints from one map (`lib/nav.ts` `GATED_DESTINATIONS` → `LockChip` in TopNav/BottomNav/GlowSubNav, accessible names "Coach Pro" / "Insights Pro" / "Simulator Pro"); dashboard card split into "Pro unlocks" and "Elite only" with a per-row chip and a "See Pro & Elite" CTA; "Upgrade to Pro" added to /arc; "Upgrade to Elite" added to /glowups; Explore's Glow-Ups card carries an Elite chip and states the feed is free; /plan's 403 toast no longer names a tier. Backend gates deliberately unchanged — the four unenforced features are documented as a business decision (gate or relabel). | `e2e/tier-clarity.spec.ts` (5 tests) |
| DEF-013 | P3 (copy) | The ≥80 score band was labelled "Elite symmetry" (frontend `scoreLabel()` + two landing mocks). "Elite" is a paid plan name, and the band is shown to free members on their own report — it reads as a tier they already own. | Renamed the band to "Exceptional symmetry" (frontend + spec). The backend `score_labels` scale (Apex · Outstanding · High Appeal · Great · Good · Average · Developing · Beginner) never used "Elite" for a band. | Copy review + `e2e/tier-clarity.spec.ts` (no "Elite" outside plan contexts) |
| DEF-014 | **P0 (trust)** | Production showed **"Symmetry 0"** on a real face (Skin 67 / Jawline 43 / Eyes 37 were plausible, so only symmetry looked broken). Chain: `POST /photos/analyze/{photo_id}` — the endpoint the web upload flow calls — accepted any `success: true` landmark result, and `detect_face_landmarks` returned `success: true, mock: true` **plus 468 fabricated ellipse landmarks** whenever MediaPipe was unavailable (missing/truncated model, or the 512 MB worker's memory guard). The ellipse's "mirror" pairs are not mirrored, so every pair measured ~0.4 of the image width and `100 - dist × 250` clamped to **exactly 0**, while jawline/eyes landed at 42.9/37.4 — i.e. the reported numbers (0 / 67 / 43 / 37) are the mock signature. `calculate_symmetry` also invented defaults (70.0 for missing input) and the results UI rendered a stored `0` as if measured. Reproduced exactly with the pre-fix code: `SYMMETRY=0, JAWLINE=42.9, EYES=37.4`. | Fail closed everywhere. `face_service`: the unavailable path now returns `success: false` + `landmarks: []` + a reason (never synthetic geometry); `landmark_measurement()` classifies every result as `measured`/`unavailable`; `calculate_symmetry` returns `None` when it cannot measure (missing/insufficient landmarks, or an implausible mirror distance > 0.25) instead of 0/70. `/photos/analyze/{id}` refuses a non-measured result with a 422, writes `analysis_status="failed"` + `analysis_details.landmark_measurement="unavailable"`, and persists **no** scores or plan. `background_analysis` applies the same rule (and now marks an upload with no score at all as `failed` with a user-facing reason, instead of completing with `score=NULL` plus a plan generated from a fabricated 50). `GET /analysis/{id}` treats ≤ 0 as not-measured and only falls back to the category breakdown when landmarks were not explicitly unavailable. `main.py` adds a one-time repair (`symmetry_score <= 1`, other categories `<= 0` → NULL). UI: `CategoryBar` renders "—" + a tooltip for an unmeasured row (never a 0) and the Results breakdown explains why; `/health` now reports `mediapipe: {available, model_path, model_path_exists, reason}`. | `backend/tests/test_symmetry_pipeline.py` (13 tests, incl. the exact old mock ellipse → `None`, a degenerate set → `None`, a realistic face → 70-100, `/analyze` 422 with NULL scores + no plan row, and `GET /analysis` never returning 0); `frontend/e2e/score-clarity.spec.ts` (unmeasured row shows "—", explained). Backend suite 323 passed. |
| DEF-015 | **P1 (billing/trust)** | Users were **charged £1 while the UI said £9.99**. The backend was correct (verified live: `CheckoutIn.first_month_offer` deployed, `has_used_first_month_offer` in `/auth/me`, coupon `FIRST_MONTH_1` = `amount_off 899 gbp / duration once`, Pro = `999 gbp`) — the failures were all presentation: (a) the £1 offer was a detached banner *above* a Pro card still headlining £9.99/mo with a "Start Pro" CTA, so the dominant number was wrong; (b) the copy/price were hardcoded in `constants.ts` instead of coming from Stripe, so nothing could detect drift; (c) there was **no confirmation UI at all** — `success_url` pointed at `/dashboard?upgraded=1` and no frontend code handled it (grep: zero hits), so a user who paid £1 saw neither the amount nor a receipt. | Added server-authoritative pricing: `GET /payments/offer` returns eligibility + both amounts read from the live Stripe price/coupon (`source: "stripe"`, `verified: true`; degrades to documented config values with `verified: false` when Stripe is unreachable, never a 5xx). `success_url` now carries `?session_id={CHECKOUT_SESSION_ID}` and the new `GET /payments/checkout/{session_id}` returns the **actual** charge, discount, next amount and next date from Stripe, ownership-checked against the session's `user_id`/`client_reference_id`. The upgrade page renders the offer *inside* the Pro card (headline £1, struck-through £9.99, "Then £9.99/month from month 2", CTA "Start for £1") only when the server says eligible, surfaces the monthly-only offer in the default annual view, and notes when the offer is already used. New `/upgrade/success` page prints the charged amount, the discount and the next payment date. | `backend/tests/test_payments.py` +11 tests (offer eligible/used/subscribed/unconfigured/Stripe-outage; receipt first-month vs full price, ownership 404, unknown session 404 — with **attribute-only Stripe stubs**); `frontend/e2e/score-clarity.spec.ts` (6 tests: £1 headline, annual hint, used-offer, no-offer, receipt £1.00 + discount, full-price receipt). Live test-mode probe: Stripe reports `amount_total=100`, `total_details.amount_discount=899`, `coupon=FIRST_MONTH_1`, and the receipt endpoint returns `amount_charged: 1.0 / amount_discount: 8.99`. The live run also caught a real crash — `metadata.get()` on a `StripeObject` (not a dict) 500'd the new endpoint — now fixed and pinned by the attribute-only stubs. |
| DEF-016 | **P1 (billing/trust)** | Follow-up to DEF-015: the £1 first month was still **invisible on landing** for a brand-new user. `/upgrade` defaults to the **annual** view, but the coupon prices Pro *monthly* only — so an eligible member's first paint was the annual card (headline £4.20/mo, list £9.99 struck through, CTA **"Start Pro"**) with the offer demoted to a small underlined link. Reproduced live (localhost:3000 → Render API, brand-new free account, `has_used_first_month_offer: false`): `GET /payments/offer` returned `eligible: true, reason: "eligible", source: "stripe", verified: true, first_month_amount: 1.0, regular_amount: 9.99`, `/auth/me` said `has_used_first_month_offer: false`, and the monthly view *did* render £1.00 — i.e. **the API, the coupon and the eligibility flag were all correct**; DEF-015's "surface it in the annual view" mitigation just wasn't enough for the default state. | **Frontend only** (`upgrade/page.tsx`): (a) an eligible free member now **defaults to the monthly view** — `useEffect` flips the toggle once eligibility is known, and never again once the visitor picks a view themselves (`pricingTouched`), so the first thing they see is £1.00 first month / £9.99 struck / "Then £9.99/month from month 2" / CTA **"Start for £1.00"**; (b) the annual-view offer is now a full-width gold **button** ("Switch to monthly for a £1.00 first month") rather than a link nobody scanning a price finds; (c) while `/payments/offer` is in flight the Pro card prints "Checking your price…" instead of quoting £9.99 and then replacing it — the DEF-015 mismatch played as a flash. | `frontend/e2e/score-clarity.spec.ts` — offer block grew to **6 tests** (landing £1 with zero interaction; annual keeps the offer one tap away; a visitor's own view choice is never overwritten; no price printed before the server answers; used offer; no offer configured) — **6/6 passed**. Live re-check localhost → Render: landing = £1.00 + "Start for £1.00"; annual = £50.40/yr + the switch button. NOTE for go-live: the coupon exists in **test** mode only (prod is still on test keys — every session is `cs_test_…`), so `FIRST_MONTH_1` + the price IDs must be recreated in live mode or the offer will silently vanish again. |

| DEF-017 | **P1 (billing/trust)** | Follow-up to DEF-016: the £1 offer had been moved *inside* the Pro price card, where it read as a footnote ("coupon on top horizontal format is disappeared" — the brief also asked for the banner back) — **and** checkout was reported as failing. Investigated live against production (localhost:3000 → Render): `POST /payments/checkout {tier: pro, annual: false, first_month_offer: true}` returns **200 in 0.79 s** with a `cs_test_…` URL; `GET /payments/checkout/{id}` reports `amount_charged: 1.0 / amount_discount: 8.99 / first_month_offer: true`; the hosted page renders "Subscribe to Pro Monthly · £1.00 · First Month £1 −£8.99 · Total due today £1.00". The one failure observed — Stripe's *"This link is incomplete / CheckoutInitError: apiKey is not set"* — was **my own probe, not the product**: the first navigation used a URL with the `#fidnandh…` fragment stripped (that fragment carries the publishable key — base64 → XOR 5 → `{"borderStyle":"default","gv":0,"apiKey":"pk_test_51UDnBm…"}`), and every later load in that tab reproduced it while the *same* URL rendered correctly in a fresh tab, a fresh context, and a fresh context seeded with the same cookies + localStorage. Two real gaps remained: backend `except Exception:` around `Session.create` logged **nothing**, and an unverified coupon priced silently. | **Frontend + backend.** (a) `upgrade/page.tsx`: the offer is a **full-width gold banner above the cards** — "🔥 FIRST MONTH SPECIAL / Get started for £1.00 / …then £9.99/month from month 2", CTA "Start for £1.00" pinned to the monthly Pro checkout (`annual: false` passed explicitly, so a visitor left on the annual toggle cannot buy the annual plan from a button promising £1). The cards are clean again (Pro price block = £9.99/mo monthly, £4.20/mo annual — no £1 headline, no in-card "switch to monthly" button), while the card CTA in the monthly view still states the amount charged. (b) The banner renders **only** for `eligible && verified`; `verified: false` now prints "The £1.00 first-month offer is temporarily unavailable — the plans below are at their regular price", so the live-mode coupon gap degrades into honest list pricing instead of a broken promise. (c) Animations (`globals.css`): banner slide-down + fade (0.5 s), card stagger (0.1 s each) + hover lift, CTA pulse — the pulse is **finite (2 cycles)** because an endless transform fails Playwright's "element is stable" click check and drifts the tap target under a thumb; all disabled under `prefers-reduced-motion`. (d) `payments.py`: `Session.create` failures now log the exact Stripe error (`type`, `code`, `param`, `request_id`) instead of being swallowed; a rejection that names the coupon/discount is retried **once at list price**, returns `offer_applied: false`, and never masks an unrelated failure (a dead API key still 502s). | `frontend/e2e/score-clarity.spec.ts` — offer block is now **9 tests** (banner on landing with zero interaction; banner CTA posts `{tier: pro, annual: false, first_month_offer: true}`; annual view still sells it; a visitor's toggle is never overwritten; the offer slot holds its place and the card price never changes; used offer; **unverified offer announced as unavailable**; Pro subscriber sees the Elite upgrade and no banner; Elite subscriber gets "Current plan" and no CTA) — **15/15 passed**. Backend `tests/test_payments.py::TestCheckoutCouponFallback` (coupon rejected → list-price retry + `offer_applied: false`; coupon accepted; non-coupon failure not retried; expired coupon falls back) — **compiles; not runnable on this Mac** (no backend venv). Live (localhost → Render, clean browser contexts): free member = banner + £1.00; banner CTA → Stripe "Total due today £1.00"; Elite subscriber = "Current plan", no banner. Open: the £1 test charge itself was **not** re-run (Stripe's card fields sit in nested `js.stripe.com` frames behind hCaptcha) — the grant path is covered by `test_payments.py::TestStripeWebhook::test_checkout_completed_grants_and_consumes_first_month_offer`. |

## OPEN (deferred / known)

| ID | Severity | Description | Status |
|---|---|---|---|
| DEF-005 | P3 | 653 deprecation warnings in backend (`datetime.utcnow()`, Pydantic class-config). Cosmetic; migrate to timezone-aware datetimes / `ConfigDict` opportunistically. | OPEN (pre-existing) |
| DEF-006 | P3 | Rate limiter + login throttle are in-memory (per-worker, reset on restart). Correct for single-instance; needs Redis-shared state for horizontal scale. | OPEN (pre-existing, documented in CONTEXT.md) |
| DEF-009 | P1 (config) | **Stripe webhook endpoint is registered to the Render production URL** (`https://lookmaxx-api.onrender.com/api/v1/payments/webhook`), not the local backend. A checkout completed against `127.0.0.1:8000` is charged by Stripe, but the events are delivered to Render and never reach local SQLite — so the local user silently stays `free` ("I paid but nothing changed in the app"). Confirmed: local DB showed the two test users as `free` with `subscription_customer_id = NULL` while Stripe held their customer + subscription. | OPEN — environment/deployment config, not an app defect. Mitigations: (a) `stripe listen --api-key … --forward-to http://127.0.0.1:8000/api/v1/payments/webhook`, then set that CLI's `whsec_…` as `STRIPE_WEBHOOK_SECRET`; (b) reconcile an already-charged user with `backend/scripts/stripe_sync.py <email|customer_id>`. Re-running the sync returns `{"duplicate":true}`, confirming `stripe_events` idempotency holds. |

## Limitations (not performed — honest)

- **pip-audit**: NOT run — the backend `.venv` has no `pip` module and pip-audit is not installed. Python deps are pinned in `requirements.txt`; a proper OSV/pip-audit scan is a recommended follow-up.
- **OWASP ZAP / DAST**: NOT configured (not installed). Manual HTTP checks (headers, CORS, throttle, information-disclosure) used as the available alternative.
- **CodeQL**: NOT configured (repo has no `.github/workflows`).
- **k6 load testing**: NOT installed; performance checks limited to load-time/latency observation.
- **Interactive Playwright MCP exploration**: NOT possible in this chat session (MCP tools absent); automated Playwright CLI E2E used instead.
